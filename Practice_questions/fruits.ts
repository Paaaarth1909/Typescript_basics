/* Declare an empty array fruits and push strings "apple", "banana", and "orange" into it. Print the array. */
let fruits: string[] = [];

fruits.push("apple", "banana", "orange");

console.log(fruits);
